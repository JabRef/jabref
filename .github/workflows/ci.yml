name: CI

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java-version: [17, 21, 24]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java-version }}
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Ensure journal-list.mv exists
        run: |
          if [ ! -f "src/main/resources/journals/journal-list.mv" ]; then
            # Download or generate the file here
            echo "Missing journal-list.mv" && exit 1
          fi

      - name: Build (assemble + tests)
        run: ./gradlew build --no-daemon --info

      - name: Upload build artifacts (optional)
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: build-libs-${{ matrix.java-version }}
          path: build/libs/**

      - name: Upload test reports (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports-${{ matrix.java-version }}
          path: build/reports/tests/**

      - name: Publish JUnit test results (optional)
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: 'Gradle Tests'
          path: build/test-results/test
          reporter: junit
