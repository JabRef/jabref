# Graceful Git error handling
`req~git.graceful-error-handling~1`
Git commit, push, and pull operations shall present concise user-facing error messages when Git operations fail, while technical exception details remain available for diagnostics.

Needs: impl

<!-- markdownlint-disable-file MD022 -->