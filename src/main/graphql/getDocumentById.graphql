query getDocumentById($userDocumentId: ID!) {
	userDocument(id: $userDocumentId){
  id
  citationKeys
  lastModified
  added
  title
  subtitle
  titleAddon
  abstract
  authors {
    ... on Person {
      name
      id
    }
    ... on Organization {
      id
      name
    }
  }
  note
  languages
  publicationState
  keywords

}
}
