query UserChanges($id: ID!) {
    user(id: $id) {
        id
        changes {
            edges {
                node {
                    id
                    # TODO: Implement revision on remote
                }
            }
            pageInfo {
                hasNextPage
                endCursor {
                    id
                    lastModified
                }
            }
        }
    }
}
