package org.jabref.jabmap.model;

/**
 * This enum represents all the icons that can be on a mindmap node
 * TODO: Add complete list when established
 */
public enum NodeIcon {
    HAVEREAD,
    PRIORITY
}
